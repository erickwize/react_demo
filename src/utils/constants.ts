const AUTH_STORAGE_KEY = 'wa_cert_authenticated';
const FAVORITES = 'favorites';
const HOME_URL = '/';
const FAVORITES_URL = '/favorites';

export { AUTH_STORAGE_KEY, FAVORITES, HOME_URL, FAVORITES_URL };
